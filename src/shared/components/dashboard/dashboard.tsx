"use client";

// Marketplace and broker dashboard rebuilt in Sprint 2.
// Props are accepted for backwards-compat with existing callers.
export default function Dashboard(_props?: {
  ownDashboard?: boolean;
  shortDashboard?: boolean;
  dashboardLink?: boolean;
  hasRefresh?: boolean;
  hasFilters?: boolean;
}) {
  return null;
}
